const AppSnippetStyles = () => ({
  root: {
    minHeight: 100,
    position: "relative" as "relative",
    border: "1px solid lightGrey",
    borderRadius: 5,
    padding: 5
  }
});

export default AppSnippetStyles;
